# TVGSUOS — Continuity Log

Persistent, dated implementation log for this repo. Read this first in any new session, before `docs/MASTER_DIRECTION.md` — this file tells you what's actually true right now; the master docs tell you the baseline it's built against. See `docs/HANDOFF_PROTOCOL.md` for the full read/write protocol this log follows.

---

- **2026-07-18: repo created, Phase 0 shipped.** Founder decided (in a `katiwala-owner-os-` session) to separate TVGSUOS into its own repo now, rather than continuing to build it as a tab inside `katiwala-owner-os-`. `github.com/hdashadm-afk/TVGSUOS` already existed (created 2026-06-10) but was empty — this session populated it.
- **Phase 0 (docs only) complete:** `README.md`, `docs/MASTER_DIRECTION.md`, `docs/GOVERNANCE_MODEL.md`, `docs/PORTFOLIO_MAP.md`, `docs/HANDOFF_PROTOCOL.md`, this file. Explicitly no app code yet — no `index.html`, `api/`, migrations, or Vercel config. Do not add any of those until the founder says so.
- **Founder-confirmed decisions baked into these docs:** four-layer ecosystem (TVGSUOS → Founder's OS → KOS/Katiwala AI → ODO/OWDO); 9 governance areas (Founder's Space, Strategy, Finance, Marketing, Product, R&D, Security/Privacy/Compliance, Intelligence, People/Admin/Governance); build-priority order for the 8 not-yet-built areas (Strategy, Product, Security/Privacy/Compliance, Marketing, R&D, then Finance/Intelligence/People-Admin-Governance); Ver/Kath/Vera hierarchy (Ver above Kath and Vera, non-redundancy rule).
- **Not done, flagged as open:** `katiwala-owner-os-`'s own `docs/TVGSUOS_MASTER_DIRECTION.md` (predates this repo, sits on an unmerged branch there) has not been updated to point here — this repo's `docs/MASTER_DIRECTION.md` declares itself canonical, but the other repo doesn't yet reflect that. Also: a `tvg-founder-os` Vercel project was deployed earlier in the same session under an interim name, before the founder corrected the name to TVGSUOS — it's live but orphaned/unlinked to this repo, harmless to leave or delete.
- **Next step, founder's call, not decided here:** what Phase 1 is. Candidates on the table: (a) start actual app code/deploy scaffold (`index.html`, Vercel config, wiring to `theverifiedgroup.org`) — explicitly out of scope until told otherwise; (b) build the first priority governance area (Strategy) as real UI/workflow; (c) reconcile `katiwala-owner-os-`'s docs to point here; (d) something else entirely. Do not assume any of these without being told.
- **2026-07-18, same day: Phase 1 scaffold shipped and deployed.** Added `index.html` + `vercel.json` (static single-file page matching the brand/type system, showing governance-area status and links out to KOS). First deploy attempt used a direct file-deploy tool (`tvgsuos` / `tvg-founder-os` project names) — **do not use or reference these**, they don't register as real, dashboard-manageable Vercel projects (confirmed: live URL worked but the project never appeared in the team's project list or API). **The real deployment is `tvgsuosweb`**, created properly via Vercel's Import Git Repository flow — GitHub-connected, auto-deploys from `main` on every push. Live at `tvgsuosweb.vercel.app`. `docs/PORTFOLIO_MAP.md` updated to match.
- **Founder chose Phase 2: build Strategy**, the #1 priority governance area, as a real feature (not just docs). Grounded in the actual shared schema before writing anything: TVGSUOS uses the same Supabase project as `katiwala-owner-os-` (confirmed `SUPABASE_URL`/anon key in that repo's `index.html`), which already has a `projects` table (id/name/slug/stage/focus_pct) and a `decisions` table (project_id FK, title, context, expected_outcome, status, confidence, actual_outcome) with RLS open to any authenticated user. Auth is Supabase password sign-in (`signInWithPassword`) — same credentials will work here, but the founder will need to log in again since each Vercel deployment is a separate browser origin (no shared session across `katiwala-owner-os.vercel.app` and `tvgsuosweb.vercel.app` even though it's one Supabase project).
- **Founder confirmed the DB write.** Migration `096_tvgsuos_project.sql` added to `katiwala-owner-os-` (its repo, since that's where the shared schema/migrations live) and applied live via Supabase MCP: one additive row, `('TVGSUOS', 'tvgsuos', 'MVP_BUILD')` into `projects`. No changes to existing rows/tables.
- **Strategy shipped.** `index.html` rebuilt: whole page is now login-gated (Supabase password auth) per the founder-only/no-public-pages rule in this repo's own mission — previously the landing page was visible with no auth at all, which didn't match that. Behind login: a Strategy section reading/writing the shared `decisions` table, rolled up across every venture in `projects` (not scoped to one venture — that's what makes it a portfolio-level Strategy view rather than a duplicate of what KOS's Founder Crossroads already does), plus a form to log a new decision against any venture, defaulting to the new `tvgsuos` row for genuinely cross-venture calls. No new tables. Governance-area grid on the page updated to show Strategy as built.
- **Verified end-to-end by the founder, live:** signed into `tvgsuosweb.vercel.app` (used the browser-autofilled password already saved from `katiwala-owner-os-` — same account, no reset needed), Strategy section loaded, logged a test decision, it saved and appeared in the list. **Strategy (governance area priority 1) is confirmed working, not just deployed.**
- **Forgot-password gap closed.** Login screen now has the same "Forgot password?" → email → reset-link flow as `katiwala-owner-os-` (`auth.resetPasswordForEmail`, redirects back to this same origin, `PASSWORD_RECOVERY` auth event now handled with a set-new-password form). Simpler than that repo's version — no `must_change_password`/`app_users` concept here, pure Supabase-auth. **Not yet verified live** (the actual send-email → click-link → set-password round trip hasn't been tested against production — flagged, not confirmed).
- **Founder chose Product (priority 2), built.** Read-only cross-venture rollup — every row in `projects` left-joined with `project_status`, showing stage/build_readiness/research_depth/last-updated in one table. Deliberately not a second decision log or venture-specific feature tracker — scoped exactly to "cross-venture product direction and consistency" per `docs/GOVERNANCE_MODEL.md`'s ownership table, non-redundant with Strategy. No new schema — `project_status` was already readable to any authenticated user. Governance-area grid updated (Product now shows built). JS syntax-checked and tag-balance-checked before commit; **not yet verified live in a browser** — founder should confirm the table renders and shows real venture data once signed in.
- **Product verified live by founder** (screenshot) — table rendered correctly with all 11 project rows, sorted alphabetically. One "gap" reported turned out not to be one: no "KOS" row exists because migration 041 (`katiwala-owner-os-`) renamed that row's `name` to "Helium Fuels" back when the app moved from the generic software name to the real gas-station brand — `slug` is still `kos`. Explained, not changed.
- **Founder then asked to declutter the rollup**: hide WP OS, Cofeshop, Gas Ops, Verde Cafe, Green Apple Hotel now that focus is KOS/Helium Fuels only. Implemented as a **client-side filter by slug** (`PRODUCT_ROLLUP_HIDDEN_SLUGS`) — the underlying `projects` rows in the shared database are untouched, matching the existing archive-don't-delete precedent for CafeVerified/HotelVerified. This only affects what TVGSUOS's Product view shows; `katiwala-owner-os-`'s own Founder Crossroads status table (same `projects` table) is unaffected. **Not yet verified live.**
- **Founder replaced the hidden-slug filter with an explicit 7-row structure**, superseding the entry above: `PRODUCT_ROLLUP_HIDDEN_SLUGS` removed; new `PRODUCT_ROLLUP_ROWS` shows, in order — TVGSUOS, Kos CEO Space, Kos StaffVerified, Kos PnLVerified, Kos StationVerified, Odo Property/TV, Odo Vehicle/RV. This is KOS's four connected modules (per `katiwala-owner-os-`'s `docs/KATIWALA_GAS_STATION_MASTER_DIRECTION.md` Product Architecture section) and ODO's two verticals (per that repo's `docs/MASTER_DIRECTION.md` — Property = TV, Vehicle = RV) named explicitly, instead of raw venture rows. **Display-only, no schema change**: CEO Space and PnLVerified both pull from the single `kos` `projects` row (they're modules within one venture, not separate ones — so they intentionally show identical stage/status, which is accurate, not a bug); StaffVerified/StationVerified/TV/RV are real separate rows, just relabeled to their branded product names (`gas_ops` → "Kos StationVerified" is the same underlying row that was hidden in the previous entry — now shown again, renamed). WP OS, Cofeshop, Verde Cafe, Green Apple Hotel, and the plain ODO row are still excluded. **Not yet verified live.**
- **Confirmed working live by founder.** The 7-row Product structure (TVGSUOS, Kos CEO Space, Kos StaffVerified, Kos PnLVerified, Kos StationVerified, Odo Property/TV, Odo Vehicle/RV) is verified, not just deployed.
- **Founder chose Security/Privacy/Compliance (priority 3), built.** Discovered before building that the "migration 088" this ecosystem's other docs reference (adding `vera_status`/`priorities`/`governance_risk_note` to `daily_baseline_checks`) was never committed as an actual migration file — checked the live schema directly via Supabase MCP and confirmed those columns exist live regardless. Also found `katiwala-owner-os-` already has a working live-advisor-check pattern (`api/generate-governance-risk.js`, requires `SUPABASE_MANAGEMENT_TOKEN`) — ported it here as `api/security-check.js` rather than reusing that repo's endpoint cross-origin, since ecosystem-wide security posture is genuinely TVGSUOS's job. **New env var needed**: `SUPABASE_MANAGEMENT_TOKEN` must be added to `tvgsuosweb`'s own Vercel project settings — separate project from `katiwala-owner-os-`, so even if it's already set there it won't carry over here. Endpoint returns a clear "not configured" message until then.
- UI adds: a "Run security & compliance check" button (live, on-demand, calls the new endpoint) plus a read-only rollup of Ver's recent `governance_risk_note` entries from `daily_baseline_checks` (same shared Supabase project, no new schema — reusing the same read-access pattern Strategy/Product already rely on). **Note: as of this entry, no `daily_baseline_checks` row has a non-null `governance_risk_note` yet** (checked live — last 5 days are all null), so this log will show empty until Ver's routine actually starts filling it in; that's accurate, not a bug.
- JS syntax-checked, tag-balance-checked, API file syntax-checked before commit. **Not yet verified live** — needs the founder to add `SUPABASE_MANAGEMENT_TOKEN` to `tvgsuosweb`'s Vercel settings before the check button will do anything beyond show "not configured."
- **Founder asked for a logo — TVG gold shield, Founder OS.** Designed a shield mark (gold gradient fill/stroke, checkmark cut in TVG Navy) with a wordmark lockup ("The Verified Group" / "Founder OS"). First pass used a separate serif for the wordmark — **founder corrected this: use the same font theme as KOS/ODO**, not a new typeface. Rebuilt on the exact `-apple-system` stack and exact `#0A1D4D` Brand Blue already used ecosystem-wide, per `katiwala-owner-os-`'s `docs/BRAND_GUIDE.md` §"unified ecosystem" rule (shared logo logic/color/typography across all products). Previewed via an Artifact (multiple sizes, both lockup layouts, on light/navy/black grounds) and the standalone SVG before wiring in, per docs-first-when-locking-direction.
- **Shipped**: new `favicon.svg` (browser tab icon), shield mark added to both the login screen and signed-in app header, defined once as a shared `<defs>`/`<use>` block so the gradient isn't duplicated. JS/tag-balance checked before commit. **Not yet verified live** — founder should confirm the favicon and header mark render correctly once deployed.
- **Logo confirmed working live by founder** ("lands well") — favicon and header mark verified, not just deployed.
- **Founder chose Marketing (priority 4), built.** Checked the shared Supabase project's actual tables before building rather than guessing — found `waitlist_leads` (KOS's public waitlist, 9 real rows, RLS `authenticated` read-only via "founder reads leads" policy) and `odo_leads_snapshots` (ODO's weekly OFW/seller/service-provider counts, 0 rows currently, RLS `authenticated` read). Both already live in the same project, no new schema. Notable finding: `waitlist_leads` currently has **no dashboard at all** in `katiwala-owner-os-` — it's only manageable via a separate standalone page, `admin-waitlist.html` (that repo's own "Leads" card actually reads different, older tables: `leads_gas_station`/`leads_coffeeshop`/`leads_hotel`, the intake-form leads, not the newer waitlist). So this genuinely fills a visibility gap, not a duplicate of something KOS already shows.
- UI: two read-only tables — waitlist signups (company/name, industry, status, date) and ODO's weekly leads. ODO's table will show its honest empty state until snapshots start getting logged — not a bug. Governance-area grid updated. JS/tag-balance checked before commit. **Not yet verified live.**
- **Founder chose R&D (priority 5), built — last of the explicitly ranked priorities.** Deliberately did not invent a new "raw idea intake" table for this, even though that's arguably closer to R&D's literal definition ("exploratory work not yet assigned to a venture") — instead scoped v1 to what's real and already in the shared database: the `projects` rows Product's active-structure list doesn't reference (`wpos`, `cofeshop`, `verde-cafe`, `green-apple-hotel`), same `projects`/`project_status` join Product uses, just the complementary slug set. Read-only, no new schema. Reasoning logged as an open question below rather than silently deciding it.
- Governance-area grid updated (R&D now shows built). JS/tag-balance checked before commit. **Not yet verified live.**
- **All 5 founder-ranked priorities now built**: Strategy, Product, Security/Privacy/Compliance, Marketing, R&D. Remaining, unordered: Finance, Intelligence, People/Admin/Governance.
- **Open question for the founder**: does R&D need an actual write capability (a real "new idea, not yet a venture" backlog — genuinely new schema, unlike anything built so far) or is the read-only dormant-ventures view above sufficient for now? Not decided here.
- **Founder asked for all 3 remaining areas at once — Finance, Intelligence, People/Admin/Governance. All 9 governance areas are now built.**
  - **Finance**: reused `PRODUCT_ROLLUP_ROWS` (same venture structure as Product) but pulled `cash_pulse`/`burn_rate`/`runway_months` from the same `project_status` rows instead of stage/build_readiness — disjoint columns, no redundancy, no new schema. Confirmed real data exists before building: Helium Fuels (KOS) has `cash_pulse: "Stable — starter figure, replace with real P&L"`, `burn_rate: 45000`, `runway_months: 8`. Every other active-list venture is currently null — honest empty cells, not a bug.
  - **Intelligence**: found `improvement_signals` — a real table (3 populated rows, `authenticated` RLS) that's exactly the "Weekly Innovation Summary" `katiwala-owner-os-`'s own docs describe as "sat empty since it was built... waiting for Kath/Vera to auto-populate it." It has real data and was never surfaced anywhere — built a read-only rollup here (scope, feature area, signal, bottleneck flag, request count, attention score, status), sorted by attention score. No new schema.
  - **People/Admin/Governance**: no live cross-venture data source exists for this one (unlike the other 8). Built it as **static content**, not a database view — the actual Ver/Kath/Vera responsibility breakdown and decision-rights/escalation rules, copied faithfully from `docs/GOVERNANCE_MODEL.md` §1 and §4, rendered in the app instead of staying buried in the repo. Flagging this explicitly since it's a different kind of "built" than the other 8 — there's nothing to verify against live data, just whether the content reads right.
  - Grid rewritten: all 9 areas show built, section heading changed from "Other governance areas" to "All 9 governance areas," status badge → "Phase 4 — all 9 governance areas live," status dot → green.
  - JS/tag-balance checked before commit. **Not yet verified live** — founder should confirm Finance/Intelligence render correctly and the People/Admin/Governance content reads right.
- **Founder answered the open R&D write-capability question**: R&D should write new ideas, and explicitly connected it to Intelligence's Weekly Innovation Summary ("I think this link to the weekly innovation summary, from Vera and Kath"). Checked the 3 existing `improvement_signals` rows before building — all literally say "Starter placeholder signal — replace with a real rollup" (`signal_type: 'recurring_request'`), confirming FOUNDER_OS.md's own earlier note that this table was seeded but never populated.
  - Implemented per the founder's stated architecture, not a separate table: R&D's new "Log a new idea" form inserts into `improvement_signals` (the same table Intelligence reads) with `signal_type: 'idea'` — distinguishing founder-originated ideas from whatever Kath/Vera eventually auto-populate, without forking into a second signals table. Submitting an idea refreshes Intelligence's rollup immediately so the link is visible in the app, not just in a doc. Intelligence's table now shows `signal_type` as a badge ("R&D idea" vs the raw type) so the two sources stay readable once mixed.
  - No new schema — `improvement_signals` already had the `signal_type` column, just unused beyond the one seed value. RLS already permitted authenticated insert ("founder can manage improvement_signals," `ALL`).
  - JS/tag-balance checked before commit. **Not yet verified live.**
- **Next step, founder's call:** verify the idea → Intelligence link works live (log a test idea in R&D, confirm it shows up in Intelligence with the "R&D idea" badge), verify Finance/Intelligence/People-Admin-Governance render correctly, add `SUPABASE_MANAGEMENT_TOKEN` to `tvgsuosweb`'s Vercel env vars and verify the security check runs, verify the forgot-password flow if not already done. All 9 governance areas have real implementation now — future work is deepening/refining them, not building from zero.
- **Header lockup rearranged**: "The Verified Group" (small label) / "TVG" (large heading, replaces the old "TVGSUOS" display text) / "Founder OS" (subtitle), stacked below the shield mark on both the login screen and app header. Visual only — app/repo name stays TVGSUOS everywhere else. Then the app-header description line ("The founder command layer for...") was dropped entirely to save space, per founder request ("I understand it already").
- **All sections converted to a collapsed-by-default accordion.** Founder asked to see everything at a glance, then expand only what needs a closer look, rather than scroll a long fully-expanded page. Every `<section>` (Strategy through Governed ventures — 11 total) now renders as a clickable header + chevron with its content hidden until clicked; data still loads eagerly on sign-in same as before, only the display is gated.
- **New "Needs your attention" panel added at the top, always visible (not collapsed)** — founder's own framing: "like a CEO's Space, here is a founder space." Pulls open Strategy decisions and open/bottleneck-flagged Intelligence signals into one at-a-glance list. Same tables Strategy/Intelligence already load, no new schema. One real implementation note: this panel's query joins `projects(name)` directly rather than reusing the shared `projectsCache`/`projectNameById` helper Strategy's own render uses, because `loadNeedsAttention()` and `loadDecisions()` now fire in parallel from `showApp()` with no guaranteed order — reusing the shared cache would have created a load-order race (venture names could show "Unknown venture" if this panel's query resolved first). Flagging this pattern for future sections that need cross-loader data: don't rely on another loader's side-effect-populated cache without an explicit await.
- JS/tag-balance checked before commit. **None of this pass verified live yet** — founder should confirm the accordion collapse/expand works, the header lockup looks right, and the Needs Attention panel shows real open items (or the correct empty state) once signed in.
- **Founder asked "where is Ver?"** — fair question: Ver only existed as text (named in Agent hierarchy, described in People/Admin/Governance), no actual feature. Answered honestly, offered options; founder chose **"Build a dedicated Ver dashboard."** Added **Ver's Desk** — an always-visible panel (below Needs Attention, above the collapsed sections) with one line per governance area: open decisions, module status coverage (X/6 active slugs — kos/staff_verified/gas_ops/tv/rv/tvgsuos, counted once each even though kos backs both Product's "CEO Space" and "PnLVerified" display rows), last security flag date, waitlist count, ideas logged, cash-data coverage, open/bottleneck signal counts. Independent lightweight `count: 'exact', head: true` queries per stat, same tables each section already loads — no new schema.
- **Founder then asked whether Ver should also have an "Ask Ver" chat, like Ask Kath does in `katiwala-owner-os-`, "to whelp me navigate my day."** Investigated before answering: `katiwala-owner-os-` already has this real, working — `ver_queries` table (question/attachment_path/attachment_name/status/answer/answered_at/created_by/created_at, RLS `authenticated`) plus `api/answer-ver-query.js` (Anthropic API call grounded in the repo's real direction docs + today's Daily Baseline Check + recent decisions, requires `ANTHROPIC_API_KEY`). **Not yet built here** — this message thread got interrupted by two other founder requests (WP OS removal, KOS/ODO quick links) before the port was finished; picking it up next.
- **"pls remove WPOS i have this deleted already"** — checked the live database first rather than assuming: the `wpos` row was **not** actually deleted (still present, 1 linked `decisions` row, 1 linked `project_status` row). Flagged the discrepancy plainly rather than silently deleting or silently doing nothing. Founder chose to hide it from R&D's display only (`RND_SLUGS` — removed `'wpos'`, left `cofeshop`/`verde-cafe`/`green-apple-hotel`), not delete the database row — same archive-don't-delete precedent as everywhere else in this ecosystem.
- **"immediate access to KOS and ODO on the dashboard"** — the only KOS/ODO links lived in "Governed ventures," which just got folded into the collapsed accordion, so they'd become unreachable without an extra click. Added an always-visible "Open KOS / Open ODO" quick-links row right below the phase status badge, above Needs Attention. Both currently point to `katiwala-owner-os.vercel.app` (ODO has no separate live URL known in this session, same as the existing Governed ventures links) — not invented, just consistent with what's already there.
- JS/tag-balance checked before each commit. **Not yet verified live**: Ver's Desk stats, WP OS hidden from R&D, and the new quick-links row.
- **Ask Ver shipped.** New `api/ask-ver.js` — synchronous version of `katiwala-owner-os-`'s `api/answer-ver-query.js` (ask, get an answer in the same request, instead of that repo's submit-then-answered-later pending queue). Grounded in TVGSUOS's own docs (`MASTER_DIRECTION.md`, `GOVERNANCE_MODEL.md`, `PORTFOLIO_MAP.md`, `FOUNDER_OS.md`, fetched live from this repo via the GitHub API) plus live open items pulled fresh from `decisions` and `improvement_signals` at request time, so answers reflect what's actually open right now, not just static docs. Every answer is saved into `ver_queries` (same table `katiwala-owner-os-` uses) for a persistent history. UI: new "Ask Ver" panel next to Ver's Desk, always visible — question box, immediate answer, 5-item recent history.
- **New required secret, not yet configured**: `ANTHROPIC_API_KEY` must be added to `tvgsuosweb`'s own Vercel project (separate from `katiwala-owner-os-`, even if already set there). Also reuses `SUPABASE_SERVICE_ROLE_KEY`, already required by `security-check.js` — if that one's configured, this one's data-fetching half already works; only the Anthropic key is the new gap.
- JS/API syntax + tag-balance checked before commit. **Not yet verified live.**
- **Founder feedback, same day, after seeing it live (screenshot)**: "ver is taking the whole space" and "his desk isnt it redundant, why not navigate me per topic." Both fair — Ver's Desk + Ask Ver were two full-width always-visible panels, and the Desk just restated numbers the sections below already showed.
  - **Moved Ver's Desk + Ask Ver into a floating button + slide-in drawer**, mirroring `katiwala-owner-os-`'s Ask Kath assistant pattern exactly (fixed bottom-right button, drawer slides in from the right, same z-index/shadow conventions) rather than inventing a new UI convention. Only visible once signed in (`#ver-toggle-btn` hidden by default, shown in `showApp()`, hidden again in `showLogin()`).
  - **Ver's Desk stats are now navigation, not a summary.** Added `data-topic` attributes to the 8 relevant `.section-toggle` buttons (Strategy, Product, Security/Privacy/Compliance, Marketing, R&D, Finance, Intelligence, People/Admin/Governance). Each stat in the drawer is now a `<button class="ver-nav-item">` — clicking it calls `jumpToTopic()`, which finds the matching section, expands it if collapsed, closes the drawer, and scrolls it into view. Resolves the redundancy complaint directly: the Desk no longer duplicates data, it's the way *to* the data.
  - **Bonus, from the founder's own annotated screenshot**: circled the KOS/ODO quick-links and asked for a "mode" pattern like FOS/KOS/ODO. Rebuilt that row as a segmented mode-switcher tab bar — FOS shown as the active/current tab (styled filled, not a link), KOS and ODO as tabs that navigate out to their own apps. True in-page mode switching isn't possible since KOS/ODO are separate deployments, but it's now styled and positioned like one, matching what was asked for.
  - JS/tag-balance checked before commit. **Not yet verified live.**
- **Next step, founder's call:** verify the floating Ver button/drawer opens and closes correctly, verify clicking a Ver's Desk item actually jumps to and expands the right section, verify the FOS/KOS/ODO mode-switcher looks right, add `ANTHROPIC_API_KEY` (and `SUPABASE_SERVICE_ROLE_KEY` if not already set) to `tvgsuosweb`'s Vercel env vars and test Ask Ver with a real question.
- **"pls remove WP OS i dont need it here its blocking my mind."** Hiding it from R&D's slug list (earlier entry) wasn't enough — Needs Attention and Intelligence both query `decisions`/`improvement_signals` directly with no venture filter, so WP OS's one open decision ("Decide WP OS core workflow shape") and its seeded placeholder signal kept surfacing there, visible in the founder's own screenshot. Added a shared `WPOS_PROJECT_ID` constant + `isWposScope()` regex helper (`/wp[\s_-]?os/i`, catches "WP OS"/"WP_OS"/"wpos" variants) and applied it everywhere WP OS could still leak through: Strategy's full decision list, both halves of Needs Attention, Intelligence's table, and Ver's Desk's open-decision/open-signal/bottleneck counts (so the numbers stay consistent with what's actually shown, not off-by-one from a hidden row). Same archive-don't-delete precedent as every prior WP-OS-related request — filtered from every live view, the underlying `decisions`/`improvement_signals` rows are untouched.
- Syntax-checked before commit. **Not yet verified live.**
- **Next step, founder's call:** confirm WP OS is now genuinely gone from every view (Needs Attention, Strategy, Intelligence, Ver's Desk counts), alongside the still-open verification items above.
- **Founder pasted a full external executive-dashboard spec** ("Build the post-login dashboard for TVG Founder OS..." — KPI strip, 3-row story, full multi-page rebuild with sidebar nav, top bar, dedicated KOS Gas/ODO-TV/ODO-RV/Projects/Settings pages, sourced from Perplexity-assembled best-practice articles). Did not build it blind — checked the actual spec's KPI list against the real schema first and found most of the named metrics (cash position, payroll readiness, KOS daily fuel volume/margin, shift coverage, site incidents, ODO consultations/escrow/verification status) have **no live data source anywhere in this ecosystem** — building them as real cards would mean fabricating numbers, which nothing else built this session does. Also flagged that the spec's navigation/page structure is a full pivot away from the current single-page accordion app, not an addition.
- **Asked the founder two scoping questions before building, got clear answers**: (1) real data only, mark the rest "Not connected" rather than fake numbers; (2) redesign just the Dashboard (top of the current page) for now, not the full multi-page rebuild — keep the 8 governance-area sections, the floating Ver assistant, and everything else built this session as-is.
- **Shipped: Dashboard rebuilt as a KPI-strip + 3-row story**, per the spec's structure but with only real data:
  - **Row 1 (12 KPI cards)**: Open decisions, Critical alerts (bottleneck signals), Projects at risk (module status coverage), Compliance due in 7 days (new read of `compliance_items` — rollup count only; detailed compliance management stays Kath's per `GOVERNANCE_MODEL.md`'s ownership table, this is founder-visibility only, not a duplicate management UI), KOS runway/burn rate (real, Helium Fuels), ODO-TV/ODO-RV stage (real). **4 cards honestly read "Not connected"**: Cash position, Payroll readiness, KOS Gas daily volume vs target, Today's Calendar.
  - **Row 2**: Critical Alerts (bottleneck-only, split out of the old combined Needs Attention), Project Health (3 compact KOS Gas/ODO-TV/ODO-RV cards), Weekly Trend Summary (honestly not connected — no historical snapshots stored anywhere, so no real trend lines/sparklines exist; the spec's sparkline requirement was dropped entirely for this reason, not fabricated).
  - **Row 3**: Decision Queue (open decisions only), Priority Tasks (open non-bottleneck signals), Today's Calendar (not connected), **Founder Notes/Quick Capture** (new — inserts into `improvement_signals` with `signal_type: 'note'`, same table R&D/Intelligence already use, no new schema).
  - Old combined "Needs your attention" panel removed — its two halves (decisions, signals) now live split across Critical Alerts/Decision Queue/Priority Tasks instead.
  - No new tables. WP OS filtering (`WPOS_PROJECT_ID`/`isWposScope`) applied to every new query. Tag-balance/syntax checked before commit. **Not yet verified live.**
- **Deliberately not built from the pasted spec**: sidebar nav, top bar (search/notifications/profile/dark-mode toggle), dedicated Projects/KOS Gas/ODO-TV/ODO-RV/Settings pages — founder's own explicit scoping choice, not an oversight. Revisit if/when the founder wants the fuller multi-page structure.
- **Next step, founder's call:** verify the new Dashboard renders correctly live (12 KPI cards, 3-panel Row 2, 4-panel Row 3, Quick Capture saves and shows up in Priority Tasks); decide whether/when to scope the real data integrations for the "Not connected" cards (Station Verified for KOS Gas volume/margin, StaffVerified for payroll, ODO's own Supabase project for cash-position-equivalent and TV/RV case data) before building more UI on top of them.
- **Founder caught that the mode-switcher's KOS/ODO tabs both pointed at the same placeholder URL** (`katiwala-owner-os.vercel.app`) instead of their real domains. Fixed: KOS → `katiwalaai.app`, ODO → `ownerdirect.online`, per the domain roles already documented in `katiwala-owner-os-`'s `docs/VERIFIED_GROUP_MASTER_DIRECTION.md`. Founder explicitly confirmed the **separate** "Governed Ventures" section's ODO link (still pointing at `katiwala-owner-os.vercel.app`, i.e. that app's own embedded ODO tab/"project update" view) is fine to leave as-is — that's a distinct, legitimate destination from the mode-switcher's direct-to-`ownerdirect.online` admin link, not a duplicate to clean up.
- **Not independently verified**: whether `katiwalaai.app` and `ownerdirect.online` actually resolve — this sandbox's network policy blocks arbitrary domain checks (same limitation flagged earlier this session for Vercel URLs). Worth noting: `docs/VERIFIED_GROUP_MASTER_DIRECTION.md` §15 (2026-07-14) flagged `www.katiwalaai.app` as returning `ERR_CONNECTION_CLOSED` at the time — unknown if that's since been fixed. Founder should click both tabs live to confirm.
- **Next step, founder's call:** click the KOS and ODO tabs live and confirm both domains actually resolve to the right apps.
- **Founder asked: "would it be better for odo to have its project update at tvgsuos, then connect to its ownerdirect.online directly, no passing at katiwala.app anymore"** — recommended yes, since the mode-switcher fix above already sends the ODO tab straight to `ownerdirect.online`; the remaining "Governed Ventures" ODO status link routing through `katiwala-owner-os.vercel.app`'s embedded view was the one piece still indirect. **Founder confirmed, and extended it: "yes, but also, kos project update must also be at tvgsuos then connect later direct to its own domain as like odo."** So both ventures' status now live natively in TVGSUOS, not inside either app.
- **Shipped: new "Venture Updates" panel**, added to the Founder Cockpit dashboard between Row 2 and Row 3 — two cards, KOS and ODO, each with a header link straight to its own real domain (`katiwalaai.app`, `ownerdirect.online`).
  - **KOS card**: reads the shared Supabase project directly (same source Product/Finance already use) — `projects`/`project_status` for slug `kos`: stage, build readiness, cash pulse, burn rate, runway. All real, no new schema.
  - **ODO card**: ODO's status lives in ODO's own, separate Supabase project (`qhjhsdsluxgyjevbhfmk.supabase.co`), unreachable from the client with the shared anon key — so this needed a new serverless endpoint, `api/odo-status.js`, ported from `katiwala-owner-os-`'s existing `api/generate-vera-status.js` pattern. Reads `vera_signals` (last 72h: count, negative-sentiment count, topic breakdown) and the latest `health_snapshots` row (pulse score, open critical/high/medium, active/closed-7d deals) via a service-role key scoped to ODO's project.
  - **New required secret**: `ODO_SUPABASE_SERVICE_ROLE_KEY` must be added to `tvgsuosweb`'s own Vercel project settings (not the shared `SUPABASE_SERVICE_ROLE_KEY`, which is scoped to the KOS/TVGSUOS project and has no access to ODO's). Until it's added, the endpoint returns `{notConfigured: true}` and the card shows a plain "not configured" message — same graceful-degradation pattern as `security-check.js`/`ask-ver.js`, no fabricated numbers.
  - **Also this pass**: removed the shield icon from the floating Ver toggle button per founder feedback — it's now plain "Ask Ver" text, no icon.
  - JS/tag-balance/API syntax checked before commit. **Not yet verified live** — founder should confirm both cards render, and add `ODO_SUPABASE_SERVICE_ROLE_KEY` to see real ODO numbers instead of "not configured."
- **Next step, founder's call:** add `ODO_SUPABASE_SERVICE_ROLE_KEY` to `tvgsuosweb`'s Vercel env vars, confirm both Venture Update cards render correctly live, and confirm the floating "Ask Ver" button reads right without the icon.
- **Founder asked to reconcile README.md/docs/PORTFOLIO_MAP.md/docs/MASTER_DIRECTION.md with reality** — all three still described this repo's first-hours Phase 0 (docs-only, no app code) snapshot from before `a8b7563` (Phase 1 scaffold), even though the rest of this log shows the same day reached Phase 4 (all 9 governance areas live). Fixed: README's "Current phase" line, tech-stack deploy-target line, and Setup step 2; `docs/MASTER_DIRECTION.md` §3's build-status line and §8 (rewritten in place rather than silently deleted, with a note explaining why it was wrong); `docs/PORTFOLIO_MAP.md`'s TVGSUOS stage cell, §3's stale phase reference, and a new §4 pointer bullet directing future sessions to this log (not `PORTFOLIO_MAP.md`) for the current shipped-but-unverified/env-var list, so that file stops trying to duplicate something that changes every session. This file itself was left untouched — per `docs/HANDOFF_PROTOCOL.md` it's the fast-moving source of truth, the other three just needed to stop contradicting it.
- **Not done, flagged rather than fixed**: `katiwala-owner-os-`'s own `docs/TVGSUOS_MASTER_DIRECTION.md` still doesn't point here (same open item noted since this repo's first day) — out of scope for a docs-reconciliation pass confined to this repo, per `docs/HANDOFF_PROTOCOL.md` §5's "do not silently edit another repo's docs" rule.
- **Next step, founder's call:** everything listed in the "Next step" entries above this one is still open (ODO env var, Venture Update cards live-check, Ver button icon) — this pass only touched docs, no app code changed.
- **Founder asked for a "YC Snapshot" card (Product/Stage/Key Metric, YC-application framing) — first built into `katiwala-owner-os-`'s Founder Crossroads, then founder decided it belongs here instead**, native to TVGSUOS same as Venture Updates. Reverted from `katiwala-owner-os-` (its `index.html` change only — the underlying `project_status.yc_stage_label`/`yc_key_metric_label` columns, migration `094_yc_snapshot_fields.sql`, stay in the shared Supabase project since this app reads them from there too).
  - Added a new always-visible table (`YC Snapshot` label, above Venture Updates) showing Katiwala AI and OwnerDirect's `yc_stage_label`/`yc_key_metric_label` from `project_status` — one query, same shared Supabase project, no `api/odo-status.js` round trip needed since (unlike ODO's live health data) this manual field lives in the shared project's `project_status` row, not ODO's own separate Supabase project.
  - Manual entry only, same as everywhere else these two columns are used — renders "—" until the founder fills them in via Supabase's table editor. No in-app edit form, matching `cash_pulse`'s existing pattern.
  - JS/tag-balance checked before commit. **Not yet verified live.**
- **Next step, founder's call:** run `094_yc_snapshot_fields.sql` (if not already run for the `katiwala-owner-os-` attempt) and fill in `yc_stage_label`/`yc_key_metric_label` for the `kos` and `odo` rows in `project_status`, then confirm the new YC Snapshot table renders correctly here.
- **Migration `094_yc_snapshot_fields.sql` had in fact never been applied** — the founder saw "Couldn't load YC Snapshot" live, checked the shared Supabase project's schema directly, confirmed the two columns genuinely didn't exist yet. Applied via Supabase MCP (`apply_migration`, same DDL as the file). Also filled in real starter values for both rows (`kos`: "Pre-revenue, seeking YC" / "MRR, pilot conversions"; `odo`: "Pre-launch, July 28" / "GMV, listings, waitlist") — the exact values the founder had given earlier as the example to build toward, not placeholders.
- **Founder then asked for an in-app edit form instead of Supabase's table editor** ("that's not nice, can u make it accessible on the dashboard"). Added a small form below the YC Snapshot table — Product dropdown (Katiwala AI/OwnerDirect), Stage + Key Metric text inputs, Save button — that reads/writes `project_status.yc_stage_label`/`yc_key_metric_label` directly via the same `sb` client already used elsewhere (confirmed first: `project_status` already has an `authenticated`-can-`ALL` RLS policy, so a client-side update needed no new policy). This is the **first in-app-editable field on `project_status`** in this ecosystem — `cash_pulse`/`burn_rate`/etc. are still Supabase-table-editor-only elsewhere; noting the precedent in case the founder wants those made editable here too later, not assuming it.
- Switching the dropdown prefills the two inputs with that venture's current saved values (not blank), so editing reads as "update," not "overwrite blind." JS/tag-balance checked before commit. **Not yet verified live.**
- **Next step, founder's call:** confirm the Save form actually persists an edit and the table refreshes with the new value, without needing a manual page reload.
- **Founder pasted another external spec** (KOS/ODO ecosystem toggle for "the Projects view" and KPI strip, with a long list of mode-specific metrics — gas margin, payroll readiness, verification completion rate, documentation gaps, escrow/protection cases, etc.). Same discipline as the earlier pasted-spec pass: checked reality before building rather than building blind.
  - **No page is literally called "Projects."** Confirmed with the founder that the existing **Product** section (the 7-row cross-venture rollup, `PRODUCT_ROLLUP_ROWS`) is what the spec means — founder agreed, use that rather than build a new section.
  - **Checked the real schema before assuming any metric was fake.** `decisions.project_id` and `compliance_items.project_id` are real FKs — Open decisions/Compliance due can be scoped precisely per ecosystem. `improvement_signals.project_scope` is real, controlled free text (live values: `KOS`, `ODO`, `WP_OS`, `TVGSUOS`) — exact-match filtering is reliable, not fuzzy. Most of the spec's named KOS/ODO-specific metrics (gas margin, payroll/staff-ops indicators, ODO verification rate, documentation gaps, ODO-TV/RV per-listing "active projects" counts) have **no live source anywhere in this ecosystem** — kept as "Not connected," not fabricated, same rule as the rest of this KPI strip.
  - **One good find**: `api/odo-status.js`'s `health_snapshots` (already live, already used by Venture Updates' ODO card) genuinely covers "ODO risk alerts" (`open_critical + open_high + open_medium`) and is the closest honest proxy for the spec's "escrow/protection cases" via `deals_active`/`deals_closed_7d` — used both, explicitly labeled as a substitution, not a literal match.
  - **Built**: a KOS/ODO segmented toggle (reusing the existing FOS/KOS/ODO `.mode-switcher` pill styling) above the KPI strip. Defaults to KOS. Clicking it re-runs `loadProductRollup`, `loadKpiGrid`, `loadProjectHealth`, `loadCriticalAlerts` filtered to the active ecosystem (`ECOSYSTEM_SLUGS.kos = [kos, staff_verified, gas_ops]`, `.odo = [tv, rv]`) — pure view-layer filter, no data changes. The Product rollup's TVGSUOS row and any `TVGSUOS`-scoped critical alert intentionally show in **neither** mode (it's the umbrella layer, not part of either ecosystem) — a deliberate "clean lens, no mixed signals" choice, not an oversight.
  - Finance's own use of `PRODUCT_ROLLUP_ROWS` (unfiltered, all 7 rows) is untouched — the founder's ask was scoped to Projects/KPIs/Project Health/Critical Alerts, not Finance.
  - JS/tag-balance checked before commit. **Not yet verified live.**
- **Next step, founder's call:** sign in, confirm the toggle actually re-filters the Product table, KPI strip, Project Health card, and Critical Alerts list correctly in both modes, and that the "Not connected" cards read honestly rather than confusingly.
